# Python cache
__pycache__/
*.pyc

# Relay build artifacts and runtime data (relay source is a git submodule at relay/)
bin/
data/
.DS_Store
*.log
*.db

# Campaign assets are generated locally and should not be versioned
ai-engine/campaign_assets/

# Generated TTS audio cache
ai-engine/tts_audio/

# Local Python virtualenvs
.venv/
ai-engine/.venv311/

# Vault index artifacts (runtime + stray test output)
.vault_index/
.vault_embeddings_cache/
ai-engine/.test_vault_rag*/

# Codebase-memory graph index (local tooling artifact)
.codegraph/
.gstack/

# Eval replay run artifacts (corpus + frozen baselines are versioned; runs are not)
ai-engine/evals/results/
