fastapi>=0.119.1,<1
uvicorn[standard]==0.30.6
openai==1.50.0
httpx==0.27.0
pydantic>=2.12.1,<3
pydantic-settings>=2.7,<3
aiosqlite==0.20.0
websockets==12.0
rumps>=0.4.0; sys_platform == 'darwin'
Pillow>=10.0.0
# [crypto] pulls in cryptography, which pypdf needs to read AES-encrypted
# PDFs — common in commercial D&D books that campaign/importer.py ingests.
# It was previously a bare `cryptography` pin that read as an unused dep.
pypdf[crypto]>=6.14.2,<7

# Semantic Vault RAG (on by default) additionally needs
# sentence-transformers — see requirements-embeddings.txt. Without it the
# engine falls back to BM25 keyword search and says so at startup.
