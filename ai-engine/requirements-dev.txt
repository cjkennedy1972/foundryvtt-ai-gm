# Development / test dependencies for the ai-engine suite.
#   pip install -r requirements.txt -r requirements-dev.txt
# Run tests with coverage (threshold enforced via .coveragerc):
#   pytest --cov=.
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.24
# Bounds a wedged test at 60s instead of the 15-minute CI job timeout (CKP-139).
pytest-timeout>=2.4
